package finance.process

import finance.utility.PageName
import org.json.JSONObject

class Transaction {
    private val logName = PageName.TRANSACTION

    fun get(): JSONObject  {
        val finalResponse = JSONObject()
        try {

            return finalResponse
        } catch (e: Throwable) {
            return finalResponse
        }
    }
}